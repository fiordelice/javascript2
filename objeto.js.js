//Declarando um objeto e informando os atributos
//Todo Objeto declaro com {}
var carro = {
    ano:'2020',
    modelo: 'Gol',
    marca: 'VW'
}
//Para exibir o atributo do objeto faça da seguinte forma
//objeto.atributo
console.log (carro.ano + '-' + carro.modelo)
console.log(`${carro.ano} - ${carro.modelo}`)



for (var pos in listaCarros){
    console.log(carro.modelo)
}

listaFilmes.push(filme)

document.getElementById('resultado').innerHTM

for (var i in listaFilmes){
    document.getElementById('resultado').innerHTML 
}

function botaoIndicado() {
    var nrSorteando = parseInt (Math.rondom() * listaFilmes.length)
    document.getElementById('indicado').innerHTML
}
